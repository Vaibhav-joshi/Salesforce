name: Block PRs to Main

on:
  pull_request:
    branches:
      - main   # Trigger only when PR targets main

jobs:
  block-main-pr:
    runs-on: ubuntu-latest
    steps:
      - name: Fail if PR targets main
        run: |
          echo "Pull requests to 'main' are not allowed."
          exit 1