name: Quality Check

on:
  pull_request:
    branches:
      - "**"
  workflow_dispatch: # 👈 manual trigger added here

jobs:
  call-eslint:
    uses: ./.github/workflows/eslint-check.yml

  call-prettier:
    uses: ./.github/workflows/prettier-check.yml

  # call-pmd:
  #   uses: ./.github/workflows/pmd-check.yml
